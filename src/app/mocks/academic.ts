export interface Academics {
    id?: number;
    idUser: number;
    instituto: string;
    titulo: string;
    habilidades: string[]; //agregar a la base de datos ¿sí o no?
    desde: string;
    hasta: string;
    foto: string;
}